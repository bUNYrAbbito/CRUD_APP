<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>All Posts</title>
</head>
<body>
    <!-- Header Section -->
    <header class="header">
        <div class="logo-container">
            <h1 class="title">Post Manager</h1>
        </div>
    </header>

    <!-- Posts Section -->
    <main class="content">
        <h2 class="section-title">All Posts</h2>

        <% for (post of posts) { %>
        <div class="post-container">
            <h3 class="post-id">ID: <%= post.id %></h3>
            <br>
            <h4 class="post-user">@<%= post.username %></h4>
            <br>
            <p class="post-content"><%= post.content %></p>
            <br>
            <div class="post-actions">
                <a href="posts/<%= post.id %>" class="btn btn-view">View</a>
                <a href="posts/<%= post.id %>/edit" class="btn btn-edit">Edit</a>
                <form action="/posts/<%= post.id %>?_method=DELETE" method="POST" style="display:inline;">
                <button class="btn-delete" type="submit">Delete</button>
                </form>
            </div>
        </div>
        <% } %>

<br>
        <a href="posts/new" class="btn btn-new">Add New Post</a>
    </main>
</body>
</html>
